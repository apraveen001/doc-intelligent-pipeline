node_modules
dist
dist-ssr
*.local

# Env files — never commit real values.
# Keep .env.example checked in as the documented template instead.
.env
.env.local
.env.*.local
.env.production
.env.development

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

.vite
coverage

# Firebase — local CLI cache and debug logs, not needed by other clones.
# firebase.json stays committed; .firebaserc is a judgment call (see note).
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
pubsub-debug.log

.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vscode/*
!.vscode/extensions.json
.idea