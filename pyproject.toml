[project]
name = "doc-intelligent-pipeline"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "python-multipart",
    "pdfplumber",
    "httpx",
    "chromadb",
    "python-dotenv"
]

[project.optional-dependencies]
dev = [
    "pytest",
    "httpx",
]

[tool.hatch.build.targets.wheel]
packages = ["app/src/core"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"